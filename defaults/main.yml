---

php_mirror_source: "americas.rsync.php.net::phpweb"
php_mirror_cron_disabled: true
php_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
php_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
